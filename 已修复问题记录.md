# 抓娃娃机 v3.3.0 — 已修复问题记录

> 本文档只记录**经代码验证确认已修复**的问题。
> 未经代码验证的修复声称一律移至「未修复问题列表」。
> 最后更新：2026-06-04（代码审查后校正）

---

## 一、已修复问题列表（代码验证确认）

| # | 问题来源 | 根因 | 修复方式 | 回归范围 |
|---|----------|------|----------|----------|
| F1 | 验收#1 / index.html 版本号未更新 | 第16行版本号为 `20260602c`，应为 `d` | 更新为 `20260602d` | 基础设施 |
| F2 | 验收#3 / CHANGELOG 缺少最新条目 | CHANGELOG.md 缺少 `v3.3.0-build20260602d` 条目 | 补充 `20260602d` 条目 | 基础设施 |
| F3 | 验收#6 / 爪子底座厚度异常 | `claw.js` init 中 `CylinderGeometry` 高度 `0.7*f`，过厚 | 改为 `0.21*f`（30%）| 爪子外观 |
| F4 | 验收#6 / 娃娃落地高度异常 | `dolls.js` 第93行 `position.y = groundY + dollSize * 0.6`，半径应为 `dollSize*0.5` | 修正为 `groundY + dollSize * 0.5` | 娃娃初始位置 |
| F13 | 验收#57 / 得分 UI 未更新 | `main.js` 中 `window.gameScore` 未正确声明/初始化 | 统一使用 `window.gameScore` 并初始化为 0 | 得分 UI |
| F14 | 验收#58 / 抓取次数未更新 | `window.gameAttempts` 未正确初始化 | 统一使用 `window.gameAttempts` 并初始化为 0 | 次数 UI |
| F18 | 验收#68 / 娃娃初始 Y 坐标浮空 | 同 F4，`dollSize*0.6` → `dollSize*0.5` | 同 F4 | 娃娃初始位置 |
| F20 | 验收#84 / 剩余次数始终为 10 | 同 F13/F14，局部变量与全局变量混用 | 统一使用 `window.gameAttempts` | 游戏流程 |

---

## 二、修复状态汇总

| 类别 | 数量 |
|------|------|
| 已确认根因并修复（代码验证） | 8 项 |
| 声称已修复但代码未改动（移至未修复列表） | 7 项 |
| 推测根因（未修复列表中） | 见未修复列表 |

---

*最后更新：2026-06-04（代码审查校正）*
