# 回归测试清单 — v3.3.0-build20260605c

> 测试前确认：浏览器访问 `http://localhost:8000`，版本号显示 `20260605c`
> 测试方法：打开浏览器开发者工具（F12）→ Console 标签页，观察日志输出

---

## 一、U6 爪子目标投影标记验证

### U6-1：投影标记显示/隐藏

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 下降时显示标记 | 1. 按下抓取按钮<br>2. 观察爪子正下方的地面 | 地面出现**红色圆环**标记，跟随爪子移动 | ⬜ 待测试 |
| 抓取时显示标记 | 1. 爪子下降到底部（grabbing 状态）<br>2. 观察地面 | 红色圆环**仍然显示** | ⬜ 待测试 |
| 上升时隐藏标记 | 1. 爪子开始上升（ascending 状态）<br>2. 观察地面 | 红色圆环**消失** | ⬜ 待测试 |
| 返回时隐藏标记 | 1. 爪子返回出口（returning 状态）<br>2. 观察地面 | 红色圆环**消失** | ⬜ 待测试 |

**判断方法**：红色圆环应该只在 `descending` 和 `grabbing` 状态时显示，其他状态隐藏。

---

## 二、U7 娃娃标签坐标验证

### U7-1：标签显示坐标

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 标签显示坐标 | 1. 页面加载完成，娃娃生成<br>2. 观察娃娃头顶的标签 | 标签**第一行**显示娃娃名字，**第二行**显示坐标 `(x=?.?, y=?.?, z=?.?)` | ⬜ 待测试 |
| 坐标格式正确 | 1. 观察标签第二行 | 坐标格式为 `(x=X.X, y=X.X, z=X.X)`，小数点后 1 位 | ⬜ 待测试 |

**注意**：当前实现是**静态坐标**（创建时绘制），不会实时更新。这是已知限制，后续可优化为实时更新。

---

## 三、U8 retractRopeMode=zero 绳长验证

### U8-1：zero 模式绳长不重置

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| zero 模式上升时绳长为 0 | 1. 配置面板设置「回收模式」= `zero`<br>2. 抓取娃娃<br>3. 观察上升过程中绳子的长度 | 上升过程中，绳子长度**保持为 0**（爪子直接上升，无绳子） | ⬜ 待测试 |
| zero 模式下一轮绳长正确 | 1. 上一轮使用 zero 模式<br>2. 得分/掉落后，爪子回到出口顶端<br>3. 开始下一轮抓取 | 下一轮抓取时，绳子长度**恢复为配置的「甩抓绳长」值**（不是 0） | ⬜ 待测试 |

**Console 日志关键行**（zero 模式）：
```
[Claw] 重置钟摆状态（回收模式: zero，保持绳长 0）
```

**Console 日志关键行**（非 zero 模式）：
```
[Claw] 重置钟摆状态（回收模式: pendulum，绳长恢复为 0.20）
```

---

## 四、综合回归测试

### 完整游戏流程（强抓 + zero 模式）

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| zero 模式完整流程 | 1. 配置面板设置「回收模式」= `zero`<br>2. 强抓成功率 = 100%<br>3. 抓取娃娃→上升→返回→得分 | 流程顺畅，zero 模式上升时无绳子，下一轮绳长恢复正确 | ⬜ 待测试 |

### 完整游戏流程（弱抓 + pendulum 模式）

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| pendulum 模式完整流程 | 1. 配置面板设置「回收模式」= `pendulum`<br>2. 强抓成功率 = 0%<br>3. 抓取娃娃→上升→返回→掉落 | 流程顺畅，pendulum 模式上升时绳子摆动，下一轮绳长保持配置值 | ⬜ 待测试 |

---

## 五、测试结论

```
测试版本：v3.3.0-build20260605c
测试时间：2026-06-05
测试人：许清楚

U6 测试：
- [ ] U6-1 投影标记显示/隐藏：待测试

U7 测试：
- [ ] U7-1 标签显示坐标：待测试

U8 测试：
- [ ] U8-1 zero 模式绳长不重置：待测试

综合结论：
- [ ] 有 bug，需要修复：
```

---

## 六、已知问题（无）

以下 P2 问题已在 `20260605c` 中修复：
- ✅ U6：爪子目标投影标记（已修复）
- ✅ U7：娃娃标签坐标（已修复）
- ✅ U8：retractRopeMode=zero 绳长重置（已修复）

 remaining：
- ⏸ U11：娃娃落入出口触发反弹（需求待重新整理）
