# 抓娃娃机 v3.3.0 — 未修复问题列表

> 按「确认根因（代码未修复）」和「推测根因」分组。
> 2026-06-04 校正：从「已修复问题记录」移入 7 项声称已修复但代码未改动的条目。
> 最后更新：2026-06-04

---

## 一、确认根因 — 代码未修复（优先级排序）

| # | 验收项 | 根因（代码验证确认） | 修复方式 | 优先级 |
|---|----------|----------------------|----------|--------|
| U6 | #6 爪子目标投影标记丢失 | `claw.js` 中无 `clawGroundMarker` 相关代码，重构时删除后未重建 | 在 `claw.js` init 中重建 `clawGroundMarker`，在 `updateSwingPosition` 中同步位置 | P2 |
| U7 | #17 娃娃标签缺坐标 | `dolls.js` `createDollLabel()` 只 `fillText(name, ...)`，未绘制 `(x=?, y=?, z=?)` 坐标 | 在 canvas 上追加绘制坐标文字 | P2 |
| U8 | #25 retractRopeMode=zero 行为异常 | `updateReturning` 中未找到 `currentRopeLength` 重置为 `pendulumRopeLength` 的逻辑 | `updateReturning` 开始时重置 `currentRopeLength` | P2 |
| U9 | #37 爪子无闭合动作 | `claw.js` 第728行 `animateClawClose` 中仍为 `f.rotation.y = 0.8`，应绕 X 轴旋转 | 改为 `f.rotation.x = 0.8` | P1 |
| U11 | #42 娃娃落入出口时错误触发反弹 | `judgeScore` 中未找到 `state='resting'` 强制设置逻辑 | 在 `judgeScore` 判定前强制设置 `physObj.state='resting'` 并清零速度 | P1 |
| U12 | #45 强抓/弱抓判定未生效 | `judgeStrongGrab` 第415行仍为对每个娃娃独立 `Math.random()` 判定，未改为爪子级别一次判定 | 改为：先掷一次随机数决定本次抓取强/弱，所有娃娃统一结果 | P0 |
| U15 | #60 强抓提示不应包含娃娃名 | `claw.js` 第426行仍为 `showFloatText('强抓！' + strongDolls.map(...))` | 改为 `showFloatText('强抓！')` | P1 |
| U16 | #61 「松开」文字不应出现 | `releaseAllDolls` 第607行仍为 `showFloatText('松开！' + names + ' 掉落中...')` | 去掉「松开！」前缀 | P1 |
| U17 | #63 绳子长度等属性未立即生效 | `config.js` `bindEvents` 中 `input` 事件只更新 `valueSpan`，未调用 `applyConfig()` | 为每个 `range` input 事件加入实时 `applyConfig()` 调用（节流）| P1 |
| U19 | #74 娃娃落地后持续平移 | `physics.js` `checkGroundCollision` 中落地后速度清零条件可能不充分 | 落地后若 `|vel.x|<0.01` 则直接清零；检查 `onGround` 标志位 | P1 |

---

## 二、推测根因 — 需加日志确认（不直接修复）

| # | 验收项 | 推测根因 | 确认方案（加日志） |
|---|----------|----------|-------------------|
| S1 | #49 掉落时无法确认是否有抛物线 | `applyParabolicDrop` 中 `initialVelocity` 可能未正确传入 | 在 `applyParabolicDrop` 入口加日志打印 `initialVelocity` |
| S2 | #50 娃娃反弹一次后诡异平移出机箱 | `checkGroundCollision` 中落地后速度未被正确清零 | 在 `checkGroundCollision` 中加日志打印落地后 `velocity.x/z` |
| S3 | #54 娃娃在距离地面一定距离就开始反弹 | `physics.js` `groundY` 与实际地面 mesh 的 Y 位置可能不一致 | 在 `checkGroundCollision` 入口加日志打印 `dollBottom`、`groundY` |
| S4 | #42 娃娃落入出口时触发反弹 | 出口区域地面 Y 与机箱地面 Y 相同 | 在 `judgeScore` 中加日志打印娃娃 `position.y` |
| S5 | #39 偶尔出现娃娃不跟随爪子 | `grabOffset` 计算时 `swingWorldPos` 可能未更新 | 在 `update()` 跟随逻辑中加日志打印 `grabOffset` |
| S6 | #25 retractRopeMode=zero 时回收前娃娃没一起移动 | `updateAscending` 中 `currentRopeLength` 降到 0 的过程中娃娃位置未同步 | 在 `updateAscending` 中加日志打印 `currentRopeLength` |
| S7 | #37 爪子闭合动作缺失 | `animateClawClose` 中旋转轴错误（Y 轴而非 X 轴）| 在 `animateClawClose` 入口加日志打印 `finger.rotation` |

---

## 三、修复优先级建议

| 优先级 | 问题编号 | 理由 |
|--------|----------|------|
| P0 | U12 | 强抓/弱抓判定逻辑错误，核心玩法错误 |
| P1 | U9, U11, U15, U16, U17, U19 | 抓取流程关键步骤异常，UX 问题 |
| P2 | U6, U7, U8 | UI/UX 细节，边界情况 |
| P3 | S1~S7 | 需日志确认，暂不阻塞发布 |

---

*最后更新：2026-06-04（从「已修复问题记录」移入 7 项未实际修复的条目）*
